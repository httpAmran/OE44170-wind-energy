[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "windtools"
version = "0.1.0"
description = "Shared helper functions for OE44170 wind energy assignments"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
